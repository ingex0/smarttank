using System;
using System.Collections.Generic;
using System.Text;
using GameBase.BackGround.SimpleTile;
using GameBase.Helpers;
using System.Xml.Serialization;

namespace GameBase.Graphics.BackGround.SimpleTile.test
{
    public class TestDataSerialize
    {
        static public void Run ()
        {
            SimpleTileData data1 = new SimpleTileData( 10, 10, 3 );
            SimpleTileData.Save( FileHelper.SaveGameContentFile( "tileGroundData" ), data1 );

            SimpleTileData data = SimpleTileData.Load( FileHelper.LoadGameContentFile( "tileGroundData" ) );
            SimpleTileData.Save( FileHelper.SaveGameContentFile( "tileGroundData1" ), data1 );
        }
    }
}
