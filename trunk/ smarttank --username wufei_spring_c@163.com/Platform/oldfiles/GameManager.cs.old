using System;
using System.Collections.Generic;
using System.Text;
using GameBase;
using Platform.Logic.Collision;
using GameBase.Interfaces;
using Platform.Logic.Registers;
using Microsoft.Xna.Framework;

namespace Platform
{
    // 这个类只是初订，有待结构上的进一步讨论。
    // 当前可以用此类进行测试。
    class GameManager : BaseGame
    {
        public static ObjectRegister<ICollidable> ColliderRegister = ObjectRegister<ICollidable>.getInstance();
        
        protected PixCollisionStrategy CollisionManager = new PixCollisionStrategy();

        protected override void Initialize ()
        {
            base.Initialize();
            this.IsMouseVisible = true;
            Coordin.SetScreenViewRect( new Rectangle( 0, 0, ClientRec.Width, ClientRec.Height ) );
            Coordin.SetLogicViewRect( new Rectangle( 0, 0, 400, 300 ) );
        }
    }
}
