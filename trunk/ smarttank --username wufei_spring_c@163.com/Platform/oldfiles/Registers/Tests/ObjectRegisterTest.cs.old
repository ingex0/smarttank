using System;
using System.Collections.Generic;
using System.Text;
using NUnit.Framework;
using Platform.Logic.Phisics;
using GameBase.Interfaces;
using Platform.Logic.Collision;
namespace Platform.Logic.Registers.Tests
{
    [TestFixture]
    public class ObjectRegisterTest
    {
        [TestFixtureSetUp]
        public void setUp ()
        {

        }
        [Test]
        public void testSIngleton ()
        {
            ObjectRegister<ICollidable> coll = ObjectRegister<ICollidable>.getInstance();
            ObjectRegister<IPhisical> phi = ObjectRegister<IPhisical>.getInstance();
            Assert.IsFalse( coll.Equals( phi ) );
        }
    }
}
